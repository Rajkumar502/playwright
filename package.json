{
  "name": "project-dependency",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "playwright:test": "npx playwright test --shard=1/3 && npx playwright test --shard=2/3 && npx playwright test --shard=3/3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.37.1"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
